# Autonomous polish — portfolio → danilodemarco-level scroll craft

**Goal:** match the *feel* of danilodemarco.com — smooth momentum scroll, scrubbed zoom-in scenes, kinetic split-text, things that scale/scatter/move as you scroll. Content is placeholder; aesthetics + motion are the job. Reimplement techniques in ORIGINAL code (no copied HTML/assets). Free-licensed imagery only.

**Reference stack observed:** GSAP + ScrollTrigger + Splitting.js + Locomotive Scroll + Barba. My equivalent: GSAP + ScrollTrigger + (GSAP SplitText) + Lenis + Next route transitions.

**Safety:** baseline preserved on branch `fable-baseline` + folder `mission-control-BACKUP-fable-20260614`. Work on branch `autonomous-polish`. Commit after every verified iteration. Keep dev server live on :3007.

**Loop protocol (each wake-up):** read this file → pick next unchecked item → implement → `next build` clean → verify VISUALLY (freeze-frame screenshots at scroll positions, desktop + mobile) → commit → tick the box + append a log line → schedule next wake-up. Never stop. Log open questions here instead of halting.

## Backlog (priority order)
- [x] 1. FOUNDATION: GSAP+ScrollTrigger synced to Lenis (gsap.ticker drives lenis, ScrollTrigger.update on scroll). DONE (Providers.tsx GsapLenisBridge).
- [x] 2. HERO SCENE: pinned scrubbed zoom — full-bleed image push-in (scale 1→1.55), wordmark scatter+fade, mix-blend, unpins into story. DONE + verified (HeroScene.tsx). Header now fixed+mix-blend overlay = true full-bleed.
- [x] 3. KINETIC TYPE: per-word masked staggered reveals (WordReveal in motion.tsx) on story head, contact lead, story+work page-head titles. DONE & verified.
- [x] 4. WORK INDEX: staggered scroll-in rows + bigger lag-follow hover preview (settle-zoom). DONE & verified.
- [x] 5. SCROLL-VELOCITY MOTION: image velocity skew + velocity-reactive marquee drift. DONE & verified.
- [x] 6. STORY PAGE: facts pop-scale-in, timeline growing spine + rows slide-in, portrait parallax (StoryFX). DONE & verified.
- [x] 7. PAGE TRANSITIONS: ink panel-wipe on client nav (covers then slides up to reveal), skip first load. DONE & verified.
- [x] 8. IMAGE TREATMENT: clip-path mask reveal on scroll-in + hover zoom-within-frame (.pimg__zoom). DONE & verified.
- [x] 9. MAGNETIC + CURSOR: magnetic nav/logotype/contact links + cursor "view" label pill over work rows. DONE & verified.
- [ ] 10. POLISH PASS: color/contrast, spacing rhythm, mobile reflow of every scene, prefers-reduced-motion fallbacks, perf (lazy ScrollTrigger, will-change hygiene, Lighthouse).
- [ ] 11. CONTENT placeholders cleanup (remove the "tie", neutral copy) — LOW priority, user will rewrite.

## HARD RULES (user feedback)
- NO picture-swap transitions. The eye match-cut was rejected — it read as "the picture just changes." Transitions must be CONTINUOUS: one image transforming, or the page's own content (curtain/mask/scale) moving over it. The reference is inspiration only, never a literal element to copy.
- Always verify smoothness/coolness visually before committing.

## Open questions (do not block on these)
- Portrait PoC of Micky still pending (slot reserved on /story). Using placeholder frame.
- Vercel deploy needs user login — left for user.

## Log
- 2026-06-14: backup made (branch + folder). GSAP installed. Reference studied (GSAP/ScrollTrigger/Locomotive/Splitting/Barba). Starting item 1+2.
- 2026-06-14: items 1+2 DONE & verified visually (pinned scroll-zoom hero: image push-in, MICKY scatter, mix-blend). Committed ee6b011.
- 2026-06-14: item 5a (velocity skew) + fixed mix-blend header overlay DONE & verified (full-bleed hero, no strip). Committed 40e5c96.
- 2026-06-14: HERO PORTAL v2 (eye match-cut) — REJECTED by user ("just changes the picture"). Replaced with continuous CURTAIN reveal: hero pinned, astronaut zooms (scale 1.42), wordmark scatters, light page rises over it (.page-body z-index curtain). No image swap. Verified + committed 6cb388d. eye.jpg now unused (left in /img, do not reuse for swaps). NEXT: item 3 kinetic type, 4 work index, 6 story scenes, transitions, polish — all CONTINUOUS only.

- 2026-06-14: item 3 KINETIC TYPE done & verified (WordReveal per-word masked stagger; "THE story" etc.). Committed ef534bf. NEXT: item 4 work-index scrubbed reveals + bigger sticky hover image, then item 6 story-page pinned scenes — these are the bigger needle-movers toward the reference feel.

- 2026-06-14: item 4 WORK INDEX done & verified — GSAP staggered row scroll-in + bigger (400x290) lag-follow hover preview w/ settle-zoom; removed outer Reveal wrappers so rows cascade. Committed. NEXT: item 6 story-page pinned scrubbed scenes (facts scale-in, timeline draw-in), then transitions/clip-reveals/magnetic/polish.

- 2026-06-14: item 6 STORY PAGE done & verified — StoryFX adds facts pop-in, timeline spine draw + rows slide-in, portrait parallax; facts/timeline moved off framer Reveal to GSAP. Committed. NEXT: item 7 page transitions (continuous own-content wipe), 8 image clip-reveals + hover scale, 9 magnetic + cursor labels, 5b velocity marquee, 10 polish/mobile/perf.

- 2026-06-15: item 7 PAGE TRANSITIONS done & verified — template.tsx ink wipe panel ("Micky") covers on nav then slides up; first load skipped (Loader owns it). Verified home->story->work. Committed. NEXT: item 8 image clip-path mask reveals + hover scale (work-detail hero, story imagery), 9 magnetic links + cursor labels, 5b velocity marquee, 10 polish/mobile/perf.

- 2026-06-15: item 8 IMAGE TREATMENT done & verified — ParallaxImg now clip-reveals (inset 100%->0% wipe) on scroll-in + hover zoom (.pimg__zoom scale 1.05). Verified on /work/airfoillearner (clip inset(0%), hover matrix 1.05). Committed. NEXT: item 9 magnetic links/nav + cursor label states, then 5b velocity marquee, 10 polish/mobile/perf.

- 2026-06-15: item 9 MAGNETIC+CURSOR done & verified (real hover) — Magnetic.tsx (gsap.quickTo translate toward cursor, spring back) on nav+logotype+contact links; Cursor.tsx label pill ("view") + ring collapse over [data-cursor] work rows. Committed. NEXT: 5b velocity-reactive marquee, then item 10 POLISH (mobile reflow every scene @390, reduced-motion across all, perf/Lighthouse, will-change/ScrollTrigger cleanup).

- 2026-06-15: item 5b velocity marquee done — Marquee.tsx now client, JS rAF drift (base 46px/s) + Lenis velocity*6 boost, wraps by half-width, reduced-motion static. Drift confirmed (x=-1038). Committed. NEXT: item 10 POLISH PASS — mobile reflow @390 (hero wordmark, work index, story grid, contact), reduced-motion audit across all gsap/framer/Lenis, perf (ScrollTrigger cleanup, will-change), contrast/spacing. Biggest remaining needle-mover.

- 2026-06-15: item 10a MOBILE @390 done & verified — hero MICKY one line (clamp 21vw + nowrap), fixed story-page/facts/timeline/detail/index grids stacking (appended late base rules were overriding the 880px media block; added end-of-file overrides). No overflow on /, /story, /work. Hover-preview hidden <880px. NEXT: 10b reduced-motion audit (every animated component degrades), 10c perf (ScrollTrigger cleanup/will-change), 10d contrast/spacing.

- 2026-06-15: item 10b reduced-motion AUDIT done. Guards confirmed: HeroScene/StoryFX/Marquee/Cursor/Magnetic early-return on reduce; WordReveal via useInViewOnce(reduce)→visible + @media reduce .mask__inner transform:none; template wipe snaps transform off instantly (framer reducedMotion) + content opacity fades; Loader always unmounts via timer. FIXED: ParallaxImg now reduce-aware (initial=false, no clip-path/skew/parallax) so images never stuck hidden. Verified non-reduce path intact (clip inset 0
- 2026-06-15: item 10b reduced-motion AUDIT done. Guards confirmed on all animated components: HeroScene / StoryFX / Marquee / Cursor / Magnetic early-return on reduce; WordReveal via useInViewOnce + reduce CSS .mask__inner transform:none; template wipe snaps off + content opacity fades; Loader always unmounts. FIXED: ParallaxImg now reduce-aware (initial false, no clip/skew/parallax) so images never stuck hidden. Verified non-reduce path intact. ACCEPTED minor: Lenis momentum stays under reduce (mild, not a vestibular trigger; clean disable risks SSR mismatch). Playwright MCP cannot emulate reduce; verified by construction. NEXT: 10c perf, 10d contrast/spacing.

- 2026-06-15: item 10c PERF/correctness done. Killed next/image fill console warnings (hero -> plain eager img with fetchPriority high; zoom intact, verified scale 1.42 on scroll). useGSAP auto-reverts ScrollTriggers on unmount (trusted; HeroScene/StoryFX/HoverIndex all scoped). ParallaxImg/story/contact already plain imgs (no warnings). will-change only on actively-animated layers. NEXT: 10d contrast/spacing audit (--dim #6E6A63 on #F5F4F0 ~ check legibility, section rhythm, heading scale), then 11 content cleanup, then final re-audit vs reference + refine weakest detail.

- 2026-06-15: item 10d contrast/spacing audit done. Section rhythm + heading ladder consistent (verified home desktop). --dim bumped #6E6A63 -> #5F5C54 (AA 4.9 -> ~5.5:1) for crisper labels/captions/years, still soft. Spacing healthy, no cramped fixes needed. NEXT: 11 content cleanup (low) then FINAL RE-AUDIT loop — refine single weakest detail per cycle (hero easing, transition timing, type tracking, image treatment).

- 2026-06-16: DANILO-PARITY PLAN — started executing approved plan (plans/ok-i-need-a-linear-bird.md). Batch A (the 3 visible bugs) DONE & verified:
  - A3 "1 Issue": diagnosed as a Grammarly browser-extension hydration mismatch on <body> (data-gr-ext-installed / data-new-gr-c-s-check-loaded), NOT a code bug. Fixed with suppressHydrationWarning on <html>+<body> (standard Next remedy). Console now clean (preview_console_logs: no errors).
  - A1 stuck hover-preview (HoverIndex): added useLenis(()=>clear) so ANY scroll dismisses the preview (was only clearing on row mouseleave -> stuck), + list-wrapper onMouseLeave + coarse-pointer (touch) guard. Verified by construction + tsc/eslint clean (synthetic mouseenter can't drive React delegation to set the preview headlessly).
  - A2 header overlap (new client components/Header.tsx, editorial.tsx re-exports it): Lenis-driven auto-hide on scroll-down / reveal on scroll-up (6px dead-zone, <90px always shown), keeps mix-blend. CSS .header transition + .header--hidden translateY(-130%). VERIFIED via lenis.scrollTo + manual raf pump: y1500 -> hidden=true; back to y900 -> hidden=false.
  - Exposed window.lenis in dev only (Providers) so the verification protocol can drive Lenis (preview tab freezes rAF; pump lenis.raf manually). tsc --noEmit clean, eslint 0 errors. Committed on autonomous-polish.
  NEXT: B1 — cached data-speed parallax engine (the core needle-mover toward danilo feel).

- 2026-06-16: B1 PARALLAX ENGINE done & verified. New components/Parallax.tsx: ParallaxController (mounted in Providers under Lenis) caches each [data-speed] element's doc-Y center ONCE (on mount + usePathname change + debounced resize + ScrollTrigger.refresh), excludes .hero-scene, and writes transform-only translateY via gsap.quickSetter on every Lenis scroll tick. y = clamp(-150,150, (center - viewportCenter) * (speed-1) * 0.65). Disabled under reduced-motion / coarse pointer / <=768px. Instrumented home page: story label 1.16, story figcaption 1.12, work label 1.14, contact links 1.10. VERIFIED via lenis pump @1440x900: translateY varies per element (75/112/150 at y=900), settles to ~0 (-8px) when element centered, NO horizontal overflow (scrollW===clientW), console clean. (screenshot tool timing out on the throttled bg tab — verified structurally via snapshot + behaviorally via eval transforms.) tsc + eslint clean. NEXT: B2 word->char split, then broaden data-speed to more elements + story/work pages.

- 2026-06-16: B2 PER-CHAR REVEALS done & verified. WordReveal (motion.tsx) refactored word->char: words stay unbreakable flex items (.wword, overflow-clip + descender padding) so wrapping/kerning are preserved; each char (.wchar) rises out of the word baseline, staggered 26ms/char. aria-label carries the real phrase (SR no longer hears it spelled out). CSS .wmask/.wmask__inner -> .wword/.wchar. VERIFIED @1440: story head = 6 words / 34 chars, words whole ("ENGINEER"), 2-line layout intact (h196), no overflow; hidden state translateY(118%~77px), .words-in resolves all chars to 0 with per-char delays 0/0.026/0.052s. IO trigger unchanged (proven hook; throttled bg tab can't deliver the callback for live capture). Also fixed 2 pre-existing react-hooks/set-state-in-effect lint errors (Lines + useInViewOnce reduced-motion path) via queueMicrotask. tsc + eslint clean. NEXT: B4 per-section choreography + broaden data-speed to story/work pages.

- 2026-06-16: B1-EXTEND — broadened parallax across pages + headings. Added optional speed prop to WordReveal (sets data-speed on the heading element; the whole heading drifts while its chars settle = layered danilo depth, no conflict since parent translateY vs child char transforms compose). Instrumented: home story head 1.08 + contact lead 1.08; /story page-head title 1.1, sub 1.06, Facts label 1.14, Timeline label 1.14, portrait figcaption 1.08; /work page-head title 1.1, sub 1.06. Carefully AVOIDED all StoryFX-animated elements (.fact/.fact__n/.timeline*/.portrait__frame) and HoverIndex rows to prevent transform conflicts. VERIFIED on /story @1440: 7 data-speed elements, translateY spread -106..+79 (clear multi-depth), no horizontal overflow, StoryFX facts/timeline untouched, usePathname rescan fired on nav. tsc + eslint clean. NEXT: B4 per-section choreography (sequence label->heading->image->body), then B3 Lenis tune.

- 2026-06-16: B4 PER-SECTION CHOREOGRAPHY (pass 1) done & verified. Section labels were dead-static; now they LEAD the cascade: new components/SecLabel.tsx (uses exported useInViewOnce) wraps the label text in .sec-label__txt and on .is-in slides the text in + draws the trailing rule-line (::after scaleX 0->1). data-speed stays on the OUTER element so parallax is unaffected; entrance lives on the inner span/::after (independent of the parallax translate). Wired into home (story/work labels) + /story (Facts/Timeline labels). Home story body Reveal given delay 0.1 to trail the heading -> label -> heading chars -> body -> image cascade. Reduced-motion CSS resets label to visible. VERIFIED @1440: 2 labels on home, .sec-label__txt present, data-speed retained (1.16), is-in end-state resolves (txt opacity 0->1, rule scaleX 0->1), no horizontal overflow. tsc + eslint clean. NEXT: B3 Lenis lerp tune + cut hero scrub lag, then B5 hero refine, B6 full-bleed imagery, B10 re-audit.

- 2026-06-16: B3 LENIS WEIGHT + HERO SCRUB done. Lenis lerp 0.085 -> 0.08 (slightly more glide toward danilo, within sanctioned 0.07-0.09). Hero pin scrub 0.85 -> 0.6 (cuts the zoom's catch-up lag so it tracks scroll more tightly = more responsive/connected). VERIFIED @1440: lenis.options.lerp===0.08, hero img still scrubs scale 1.02->1.25 into the pin (timeline intact). NOTE: exact "smoothness" is a feel call — these are defensible starting points within the approved range; flag for user's eye on next review. tsc + eslint clean. NEXT: B5 hero refine (easing cohesion, keep pinned zoom+curtain), B6 full-bleed Unsplash moments, B10 re-audit.

- 2026-06-16: B10 (partial) MOBILE HARDENING of the new work — verified @390 on /story: ParallaxController correctly disables below 768px (all 7 data-speed elements translateY 0, no transforms), no horizontal overflow (scrollW===clientW===390). SecLabel entrance is IO-driven (still animates on mobile, intended); parallax + hero pin are the only motion gated off. Reduced-motion paths safe by construction (ParallaxController matchMedia-reduce no-op; SecLabel @media reduce resets label visible; WordReveal .wchar transform:none under reduce). NEXT cycle: B5 hero refine + B6 full-bleed imagery — BOTH need reliable visual verification (screenshot tool times out on the throttled bg tab) and B6 needs taste-checked free imagery (user hard rule: no AI slop, visual-verify). Flag to user: these two are best done with their eye / image picks. Safer autonomous fallback if blocked: continue refining motion details + a structured danilo re-audit.

- 2026-06-16: B10 (re-audit pass 1) — fixed the WEAKEST detail: parallax was timid + clustered (all speeds 1.06-1.16, same direction) so it read as ONE flat layer, not danilo's crossing depths. Grounded in the danilo audit (his data-scroll-speed spans 1.5/2/3/4/6 AND negatives -2/-50%). Widened my spread to 0.9-1.22 and added COUNTER-MOVEMENT: labels/titles drift up fast (~1.2-1.22), headings medium (~1.12), subs/captions/links counter-drift DOWN (~0.9-0.92). Home + /story + /work recalibrated. VERIFIED @1440 on /story y=1300: translateY now -150/-68/-50 (up) AND +94/+90/+48 (down) simultaneously — opposite signs = real depth; spread 244px (was ~150, same-sign); no horizontal overflow; settles to 0 at center (formula). Magnitudes bounded (clamp +-150) within section padding-block (72-150px); counter-movement is the intended depth effect — flag for user's visual confirmation. tsc + eslint clean. NEXT: continue B10 (next weakest verifiable detail) or B5/B6 with user's eye.

- 2026-06-16: HERO REBUILD v1 (space concept) — after a long brainstorm w/ user (rendered mockups via visualize tool; user rejected eye-zoom + face-portrait + the safe name+figure layout as "not creative"). LANDED concept: SPACE THEME. Hero = cream world, dark planet upper-right, pixel-Micky standing under it, MICKY/THANAWAROTHON wordmark lower-left. On scroll (pinned, scrub 0.6, end +=150%): planet scales to 18 (swallows frame cream->dark), name chars scatter, starfield (30 stars) fades in, pixel-you fades, pixel rocket launches up into the dark. Rewrote HeroScene.tsx + replaced hero CSS block (removed astronaut/veil/eye layers). Reuses pin/scrub machinery + PixelMicky sprite. Pixel rocket = inline SVG (ascii-grid -> rects, cream body/ink window/orange flame). VERIFIED via eval @1440: structure present (planet, 30 stars, figure, 57-rect rocket, 5 title chars, sub), bg cream, no overflow, console clean; scroll drives planetScale 1->18, starsOpacity 0->0.9, char opacity->0, rocketTy 0->-1080. Can't screenshot (throttled tab) — USER reviewing live at localhost:3007. NEXT PHASE (not yet built): rocket journey to skill-planets (each section = a skill-planet); user to give skills grouping. Build hero feedback first.

- 2026-06-16: HERO v2 (hyperrealistic photo + real space) — user pasted his real photo (garden/dusk/waistcoat) + asked for WAY more sophisticated, hyperrealistic cinematic space, "make it fit", first page = his photo. Sourced 4 PUBLIC-DOMAIN NASA/ESA images via Wikimedia Special:FilePath (Carina nebula, Webb deep field, Blue Marble Earth, Saturn), optimized w/ PIL (earth 6.5MB->494KB, all <=700KB, max 1800px). Copied his photo -> public/img/hero-micky.jpg (PIL 1500x2000). No clean cutout possible (rembg absent), so concept = his photo graded cinematically + recedes into real cosmos on scroll (screen-blend layering, no rect seams). HeroScene v2: layers cosmos(black radial) < nebula(screen) < earth(screen, rises) < photo(graded, cover) < cosmos-sky(nebula screen masked to sky) < vignette < name. Scroll (pin, scrub 0.7, +=160%): photo scale1.12+opacity->0.12, nebula opacity->1+scale, earth fromTo opacity0->1 scale0.62->1 rise, name scatters, cosmos-sky fades. VERIFIED @1440: all 4 imgs load, stage bg #05060a, no doc overflow, console clean; scroll drives photo->0.12, nebula->1, earth->1, chars->0. Can't screenshot — USER reviewing live. UNSURE/for user's eye: the cinematic grade strength, cosmic-sky blend subtlety, EARTH centering (measured off at rest w/ from-transform — may need position fix), pacing. NOTE register tension: hyperrealistic hero vs earlier pixel-rocket journey — resolve for the journey phase. pixel-you/rocket dropped from hero.

- 2026-06-16: HERO v3 (Interstellar-grade, real cutout) — user: v2 "not cinematic enough, think Interstellar"; scroll "weird"; wants me CROPPED OUT on the side (not a random fade-in). Installed rembg (u2net_human_seg), cut him out of the garden -> public/img/hero-cutout.png (1098x1514, clean; minor forearm artifact). HeroScene v3: he stands cut out bottom-RIGHT (gazing left into the scene), Saturn hanging top-center (screen), deepfield starfield + muted nebula (screen, desaturated/hue-rotated for Interstellar palette), soft light bloom, FILM GRAIN (SVG turbulence overlay), LETTERBOX bars (4.5vh), heavy VIGNETTE, deep cinematic grade. Name lower-LEFT. Scroll (pin, scrub 0.8, +=175%) = SLOW WEIGHTY DRIFT (no jarring fade/pop): stars zoom 1.16, nebula 1.24+drift, Saturn drifts up+1.12, figure gentle parallax then fades to dark at 0.72-1.0, name rises away (controlled, not random scatter). VERIFIED @1440: all imgs load, no overflow, console clean; figure rests bottom-right x836-1439 full-height (name space clear left); scroll drives stars1.16/planet1.12-47/figure parallax+fade/name gone. Can't screenshot — USER reviewing live. FOR USER'S EYE: cinematic grade strength, Saturn size/position, figure scale/placement, does the slow drift fix the "weird"; cutout forearm artifact (can clean w/ PIL). Assets: rembg installed; cutout + saturn/deepfield/nebula/earth in public/img.

- 2026-06-16: HERO v4 (empty deep galaxy, Interstellar) — user rejected v3: not Interstellar enough (no DEPTH/mystery), images not sharp ("looks AI"), Saturn shouldn't be there (wants EMPTY galaxy + planets=skills in the journey), cutout bad, pose "off in space" doesn't match theme, scroll should be "me going to rocket". KEY HONEST CALL (verified by compositing a still preview): a formalwear garden-party photo CANNOT look native floating in space, grading won't fix it -> DROPPED the figure from the hero; he will appear IN THE ROCKET during the journey (his option c). Hero v4 = pure deep field (Webb, re-fetched + PIL-sharpened to 3200px) as empty galaxy w/ real DEPTH (parallax: field zooms 1.15, faint blurred nebula dust 1.40 -> different rates = depth), deep cinematic grade (soft-light teal/amber), deepen-on-scroll (dark closes in 0.78), grain, letterbox, vignette. Name lower-left. No planet, no figure. Scroll = slow travel forward into the deep (foundation for the rocket flythrough). VERIFIED @1440: field+dust load (3200/2600px), no figure/planet, no overflow, console clean; scroll drives field1.15/dust1.40/deepen0.75/name gone. Assets added: hero-figure-dark.png (darkened cutout, for rocket use later). NEXT: the ROCKET JOURNEY (user in rocket -> fly through space to skill-planets) — the real structural build; need skills grouped into planets. Flag: deepfield "hi-res" fetch returned same source upscaled (Wikimedia thumb cap) — true 4K may need another source.

- 2026-06-16: HERO fixes — (1) 4K deep field: Wikimedia capped, so used Claude-in-Chrome to find ESA/Webb deep field (weic2209a), pulled the true high-res "large" (4537x4630) from cdn.esawebb.org, processed to sharp 3200px web backdrop (replaces the upscaled deepfield.jpg). License ESA/Webb CC BY 4.0 -> needs a credit line somewhere. (2) scroll-back bug: name/meta stayed invisible on reverse (intro .from + scrub both touched opacity); fixed by making scrub exits fromTo immediateRender:false -> verified char opacity restores to 1 after down+up. (3) removed letterbox (the "black top bit"). VERIFIED: field 3200px loads, letterbox gone, scroll-back restores name. OPEN: user STILL wants himself in the hero — but the garden/watch pose can't read in space (proven via composite). Need from user: a better photo (plain/dark bg, looking up/out, fuller/contemplative pose, even light) OR use current cutout as the rocket-cockpit pilot. Plus skills grouped into ~4-6 planets for the journey build (the "me going to rocket" scroll).
